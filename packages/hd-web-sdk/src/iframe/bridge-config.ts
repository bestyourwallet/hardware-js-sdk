const config: {
  iframeName: string;
  hostName: string;
  channel: string;
  scope: '$hardware_sdk';
} = {
  iframeName: 'onekey-iframe',
  hostName: 'onekey-host',
  channel: 'ukey-js-sdk',
  scope: '$hardware_sdk',
};

export default config;
