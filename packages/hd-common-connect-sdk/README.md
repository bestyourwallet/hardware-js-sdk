# `@ukeyfe/hd-common-connect-sdk`

`@ukeyfe/hd-common-connect-sdk` is a libarary that runs in the node enviroment. And communicates with transport in the normal way.

## Installation

Install library as npm module:

```javascript
npm install @ukeyfe/hd-common-connect-sdk
```

or

```javascript
yarn add @ukeyfe/hd-common-connect-sdk
```

## Initialization

```javascript
import HardwareSDK from '@ukeyfe/hd-common-connect-sdk';

function init() {
  HardwareSDK.init({
    debug: false,
  });
}
```

## Docs

Documentation is available [hardware-js-sdk](https://developer.ukey.so/connect-to-hardware/hardware-sdk/start)

## Examples
// TODO: add example url
